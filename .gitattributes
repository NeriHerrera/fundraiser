# Normalize line endings to LF for all text files
* text=auto eol=lf

# Keep common binaries untouched (safety; text=auto already skips most)
*.png -text
*.jpg -text
*.jpeg -text
*.gif -text
*.webp -text
*.ico -text
*.pdf -text
*.woff -text
*.woff2 -text

